:80 {
	root * /srv
	# This page is edited live (volume mount) — force browsers/edges to revalidate
	# every load so updates show immediately instead of serving a stale cached copy.
	header Cache-Control "no-cache"
	file_server
}
