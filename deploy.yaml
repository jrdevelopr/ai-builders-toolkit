name: agents
type: docker
domain: jrdevelopr.com     # public home = .com apex + www (moved 2026-07-02); route: caddy/apps.d/jrdevelopr-com.caddy. Old aibuilders/agents.jrdevelopr.site 301 here.
port: 8088                 # host port (0.0.0.0) -> http://192.168.20.108:8088
container_port: 80
public: true
auth: false                # OPEN on purpose — shareable comparison page, NO lab-gate
self_managed: true         # tiny caddy:2-alpine file-server; brought up manually, not deploy.sh
icon: scale-balance
# Static one-pager: "AI Builders Toolkit" — comparison of AI agent platforms, model providers,
# tools, and AI building blocks. Data lives in site/index.html (a JS array — add an entry = add one object).
